# 04 – Iteraciones: menu_iterativo.py

Menú con `while` (repite acciones) y `for` (recorre listado ejemplo).
Permite simular registros y listados simples.

```bash
python 04_iteraciones/menu_iterativo.py
```